import socket
from flask_socketio import emit, socketio
"""
class SocketInit:
    def __init__(self):
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        self.server_address = ('localhost', 5000)

def recv_img():
    size_len = int.from_bytes(self.sock, byteorder='big')
    size = int.from_bytes(self.sock, byteorder='big')
    pixels = decompress(recvall(self.sock, size))
    return pixels

def main():
    recv_img()
"""
if __name__ == "__main__":
    main()